import GameApp from "./core/GameApp.js";

// Menunggu hingga semua elemen HTML dimuat sebelum memulai script
document.addEventListener("DOMContentLoaded", () => {
  // Memulai game
  const app = new GameApp();
});
